/**
 * Placeholder for the React Query hooks Orval generates from the OpenAPI
 * spec (`pnpm --filter @workspace/api-spec run codegen`). Not generated
 * yet — `regaudit-app` doesn't import from this package until it is.
 */
export {};
