/**
 * Placeholder for the Zod schemas generated from the API's OpenAPI spec
 * (`pnpm --filter @workspace/api-spec run codegen`, per the root README).
 * No OpenAPI spec exists yet, so this package currently ships no schemas.
 * `@workspace/api-server` and `@workspace/regaudit` define their own
 * request/response types directly until codegen is wired up.
 */
export {};
